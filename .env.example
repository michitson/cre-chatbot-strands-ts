# AWS credentials are typically supplied via `aws configure` or AWS_PROFILE,
# not via this file. This file only documents app-level secrets.

# Used in Phase 4+ when the LLM-powered field parser comes online.
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
